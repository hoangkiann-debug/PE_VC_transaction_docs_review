# Security

Report a suspected secret, confidential-data exposure, unsafe file operation,
or package-integrity issue through a private GitHub security advisory. Do not
include client documents or personal data in a public issue. Release packages
include SHA-256 hashes in `release-manifest.json` for integrity checks.
